databaseChangeLog:
  - changeSet:
      id: create-passport
      author: Vadim
      changes:
        - createTable:
            tableName: passport
            columns:
              - column:
                  name: passport_uuid
                  type: uuid
                  autoIncrement: false
                  constraints:
                    primaryKey: true
                    nullable: false
                    unique: true
              - column:
                  name: series
                  type: varchar(4)
                  constraints:
                    nullable: true
              - column:
                  name: number
                  type: varchar(6)
                  constraints:
                    nullable: true
              - column:
                  name: issue_branch
                  type: varchar(200)
                  constraints:
                    nullable: true
              - column:
                  name: issue_date
                  type: date
                  constraints:
                    nullable: true